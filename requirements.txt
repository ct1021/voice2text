# 核心依赖
numpy
pyperclip
sounddevice
PyQt6

# 热键 - Windows 用 keyboard，macOS/Linux 用 pynput
keyboard ; sys_platform == "win32"
pynput ; sys_platform != "win32"

# STT - faster-whisper 引擎（默认）
faster-whisper

# STT - SenseVoice 引擎（可选，仅 config [stt] backend=sensevoice 时需要）
sherpa-onnx

# AI 清洗后端
claude-agent-sdk
anthropic
openai
